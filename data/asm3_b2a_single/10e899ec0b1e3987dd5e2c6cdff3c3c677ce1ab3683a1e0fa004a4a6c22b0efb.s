 .name fcn.0061d9b0
 .offset 000000000061d9b0
 .file fcn_win.exe
 mov eax, CONST
 call CONST
 mov eax, dword [CONST]
 xor eax, esp
 mov dword [esp + CONST], eax
 mov eax, dword [esp + CONST]
 push esi
 mov esi, dword [esp + CONST]
 movups xmm0, xmmword [eax]
 movups xmm1, xmmword [eax + CONST]
 movd eax, xmm0
 movups xmmword [esp + CONST], xmm1
 and al, CONST
 psrldq xmm1, CONST
 movups xmmword [esp + CONST], xmm0
 mov byte [esp + CONST], al
 movd eax, xmm1
 shr eax, CONST
 and al, CONST
 or al, CONST
 mov byte [esp + CONST], al
 lea eax, [esp + CONST]
 push eax
 lea eax, [esp + CONST]
 push eax
 call CONST
 mov ecx, dword [esp + CONST]
 mov edx, dword [esp + CONST]
 lea eax, [ecx + edx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov ecx, dword [esp + CONST]
 mov dword [esp + CONST], edx
 mov edx, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [edx + ecx]
 sub edx, ecx
 mov dword [esp + CONST], eax
 mov dword [esp + CONST], edx
 lea eax, [esp + CONST]
 push eax
 lea eax, [esp + CONST]
 push eax
 call CONST
 lea eax, [esp + CONST]
 push eax
 lea eax, [esp + CONST]
 push eax
 push eax
 call CONST
 lea eax, [esp + CONST]
 push eax
 push esi
 call CONST
 mov ecx, dword [esp + CONST]
 add esp, CONST
 pop esi
 xor ecx, esp
 call CONST
 add esp, CONST
 ret
