 .name fcn.0042fb96
 .offset 000000000042fb96
 .file fcn_win.exe
 push esi
 mov esi, ecx
 xor eax, eax
 fld1
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 fstp qword [esi + CONST]
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 fld1
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 fstp qword [esi + CONST]
 mov dword [esi + CONST], eax
 mov dword [esi + CONST], eax
 mov eax, dword [esp + CONST]
 mov dword [esi], CONST
 mov byte [esi + CONST], CONST
 mov byte [esi + CONST], CONST
 mov dword [esi + CONST], eax
 push eax
 mov dword [eax + CONST], esi
 call CONST
 mov eax, esi
 pop esi
 ret CONST
