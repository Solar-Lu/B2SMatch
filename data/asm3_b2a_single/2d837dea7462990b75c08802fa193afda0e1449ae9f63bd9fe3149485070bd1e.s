 .name dbg.main
 .offset 000000000040169e
 .file 1.exe
 push ebp
 mov ebp, esp
 and esp, CONST
 sub esp, CONST
 call CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov dword [esp + CONST], CONST
 mov eax, dword [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [esp + CONST]
 mov dword [esp + CONST], eax
 lea eax, [esp + CONST]
 mov dword [esp + CONST], eax
 mov eax, dword [esp + CONST]
 mov dword [esp], eax
 call CONST
 mov dword [esp + CONST], eax
 mov dword [esp], CONST
 call CONST
 mov eax, CONST
 leave
 ret
